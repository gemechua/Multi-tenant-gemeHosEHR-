import { EntityConfig } from '../schema';
import { 
  ClipboardList, BarChart3, Database, Shield, Activity, 
  FileSpreadsheet, Users, DollarSign, Settings, FileText
} from 'lucide-react';

export const MODULES_ENTITIES: Record<string, EntityConfig> = {};
